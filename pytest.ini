[pytest]
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
markers =
    bulk_register: test module registers more than 5 accounts/minute and needs the real POST /api/auth/register rate limit disabled (see backend/tests/conftest.py::_disable_register_rate_limit)
